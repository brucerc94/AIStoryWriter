# Required at application startup
PySide6>=6.5.0
cryptography>=41.0.0

# Optional — required for local language-model inference
# Install a build appropriate for your platform and GPU setup.
# llama-cpp-python

# Optional — required for DOCX export
# python-docx>=1.1.0

# Optional — required for PDF export
# reportlab>=4.0.0

# Optional — required for local image generation
# The package provides the `stable_diffusion_cpp` Python binding.
# Install a build appropriate for your platform.
# stable-diffusion-cpp-python
